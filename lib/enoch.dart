import 'package:flutter/material.dart';

class Enoch extends StatelessWidget {
  const Enoch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Hello, my name is enoch');
  }
}
