import 'package:flutter/material.dart';

class BuildALayoutPage extends StatelessWidget {
  const BuildALayoutPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: child,
    );
  }
}
